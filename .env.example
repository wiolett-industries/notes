# Development: npm run dev
ORIGIN=http://localhost:5173
RP_ID=localhost
HOST=127.0.0.1
PORT=3001
# Per-board stored payload quota in decimal MB (default: 200).
BOARD_LIMIT_MB=200
# Production: NODE_ENV=production and an HTTPS origin are required.
# DATABASE_PATH=/data/notes.sqlite
# IMAGES_PATH=/images
